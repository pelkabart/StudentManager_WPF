<UserControl x:Class="StudentManager.Views.ListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <SolidColorBrush x:Key="TextPrimary" Color="#E8EEFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A9B4D0"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#22304F"/>
        <SolidColorBrush x:Key="Surface" Color="#111A2E"/>
        <SolidColorBrush x:Key="Surface2" Color="#0F172A"/>
        <SolidColorBrush x:Key="Accent" Color="#2DD4BF"/>

        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="0" Opacity="0.25"/>

        <Style x:Key="H1" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="Muted" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="ResultsList" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBd"
                                            Background="{StaticResource Surface2}"
                                            BorderBrush="{StaticResource BorderBrushSoft}"
                                            BorderThickness="1"
                                            CornerRadius="14"
                                            Padding="14">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="#132041"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                            <Setter TargetName="ItemBd" Property="Background" Value="#14284C"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.55"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}" Text="Lista studentów"/>
            <TextBlock Style="{StaticResource Muted}" Margin="0,6,0,0"
                       Text="Wszystkie rekordy w systemie."/>
        </StackPanel>

        <!-- Count bar -->
        <Border Grid.Row="2"
                Background="{StaticResource Surface}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="12"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="14"
                           Text="Studenci"/>

                <Border Grid.Column="1"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="0"
                        CornerRadius="999"
                        Padding="12,6"
                        VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="12"
                               Text="{Binding Students.Count, StringFormat=Ilość: {0}}"/>
                </Border>
            </Grid>
        </Border>

        <!-- List -->
        <ListBox Grid.Row="4"
                 Style="{StaticResource ResultsList}"
                 ItemsSource="{Binding Students}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="15"
                                   Text="{Binding}"/>
                        <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,6,0,0"
                                   Text=""/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
