<Window x:Class="StudentManager.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Student Manager" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackground}"
        FontFamily="Segoe UI"
        FontSize="14">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="AppBackground" Color="#0B1220"/>
        <SolidColorBrush x:Key="Surface" Color="#111A2E"/>
        <SolidColorBrush x:Key="Surface2" Color="#0F172A"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#22304F"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#E8EEFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A9B4D0"/>
        <SolidColorBrush x:Key="Accent" Color="#2DD4BF"/>
        <SolidColorBrush x:Key="Accent2" Color="#38BDF8"/>

        
        <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="0" Opacity="0.35"/>

        <!-- Nav button style -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="6"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                
                                <Border x:Name="AccentBar" Grid.Column="0" Background="Transparent" CornerRadius="12,0,0,12"/>

                                <ContentPresenter Grid.Column="1"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#16233F"/>
                                <Setter TargetName="AccentBar" Property="Background" Value="{StaticResource Accent2}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1C2B4E"/>
                                <Setter TargetName="AccentBar" Property="Background" Value="{StaticResource Accent}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header text -->
        <Style x:Key="H1" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="Muted" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="18"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Border Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0"
                Background="{StaticResource Surface}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="18"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Style="{StaticResource H1}" Text="Student Manager"/>
                    <TextBlock Style="{StaticResource Muted}"
                               Text="WPF • MVVM • SQLite"
                               Margin="0,6,0,0"/>
                </StackPanel>

            </Grid>
        </Border>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Grid.Row="2"
                Background="{StaticResource Surface2}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="16"
                Effect="{StaticResource CardShadow}">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" Text="Nawigacja"/>
                    <TextBlock Style="{StaticResource Muted}" Text="Wybierz widok" Margin="0,4,0,12"/>

                    <Button x:Name="ShowSearch" Style="{StaticResource NavButtonStyle}">
                        Wyszukaj studenta
                    </Button>

                    <Button x:Name="ShowList" Style="{StaticResource NavButtonStyle}">
                        Lista studentów
                    </Button>

                    <Button x:Name="ShowAddPanel" Style="{StaticResource NavButtonStyle}">
                        Dodaj studenta
                    </Button>

                    <Button x:Name="ShowDeletePanel" Style="{StaticResource NavButtonStyle}">
                        Usuń studenta
                    </Button>
                </StackPanel>


            </DockPanel>
        </Border>

        <!-- CONTENT -->
        <Border Grid.Column="2" Grid.Row="2"
                Background="{StaticResource Surface}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                CornerRadius="16"
                Padding="18"
                Effect="{StaticResource CardShadow}">
            <Grid>
                <ContentControl x:Name="ActiveItem"/>

                
                <TextBlock Text="Wybierz widok z menu po lewej."
                           Foreground="{StaticResource TextSecondary}"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=ActiveItem, Path=Content}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
